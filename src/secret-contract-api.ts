/**
 * Secret contract. Phase 1 scaffolding — no secret-target entries registered yet.
 * Phase 4 will register entries for IMAP_PASSWORD and SMTP_PASSWORD with keyring/pass resolution.
 */
export const channelSecrets = {
  secretTargetRegistryEntries: [] as const,
};
